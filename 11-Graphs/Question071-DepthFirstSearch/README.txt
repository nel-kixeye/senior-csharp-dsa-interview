Given a graph represented as an adjacency list and a start node,
perform Depth-First Search and return the visited nodes in order.

Restrictions:
- Use a stack (iterative) or recursion
- Track visited nodes to avoid cycles
- Do not use LINQ
- O(V + E) time complexity