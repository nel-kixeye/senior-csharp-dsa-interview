Implement basic string compression using counts of repeated characters.
If the compressed string is not smaller than the original, return the original.

Example: "aabcccccaaa" -> "a2b1c5a3"
Example: "abc" -> "abc" (compressed "a1b1c1" is longer)

Restrictions:
- Do not use LINQ
- O(n) time complexity