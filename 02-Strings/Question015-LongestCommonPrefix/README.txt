Given an array of strings, find the longest common prefix shared by all strings.
Return an empty string if there is no common prefix.

Restrictions:
- Do not use LINQ
- O(S) time where S is the sum of all characters across all strings