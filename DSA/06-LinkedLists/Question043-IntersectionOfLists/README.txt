Given the heads of two singly linked lists, return the node at which they intersect.
Return null if they do not intersect.

Note: "intersect" means they share the same node by reference (same memory), not just same value.

Restrictions:
- O(m + n) time, O(1) space
- Do not use a HashSet
- Two-pointer technique preferred