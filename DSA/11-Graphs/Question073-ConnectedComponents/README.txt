Given n nodes (labeled 0 to n-1) and an edge list, count the number
of connected components in an undirected graph.

Example: n=5, edges=[[0,1],[1,2],[3,4]] -> 2 components

Restrictions:
- Use DFS or BFS with a visited set
- Do not use LINQ
- O(V + E) time complexity