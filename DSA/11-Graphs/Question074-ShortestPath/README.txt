Given an unweighted undirected graph as an adjacency list,
find the shortest path (fewest edges) between a start node and an end node.
Return the number of edges in the shortest path, or -1 if no path exists.

Restrictions:
- Use BFS (it guarantees shortest path in unweighted graphs)
- Do not use LINQ
- O(V + E) time complexity