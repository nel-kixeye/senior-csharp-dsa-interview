Given a non-negative integer x, return the integer square root of x.
The result must be truncated (floor), not rounded.

Example: x=8 -> 2  (because sqrt(8) = 2.828..., truncated to 2)

Restrictions:
- Do not use Math.Sqrt()
- Use binary search
- O(log x) time complexity