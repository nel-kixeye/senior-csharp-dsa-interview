Given an integer array, find a peak element.
A peak element is one that is strictly greater than its neighbors.
For boundary elements, treat out-of-bounds as negative infinity.
Return the index of any one peak element.

Restrictions:
- O(log n) time complexity using binary search
- Do not use LINQ