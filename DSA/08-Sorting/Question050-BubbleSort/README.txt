Implement Bubble Sort on an integer array.
Bubble Sort repeatedly steps through the list, compares adjacent elements
and swaps them if out of order.

Add an optimization: stop early if no swaps occurred in a pass.

Restrictions:
- Implement from scratch, do not use Array.Sort() or LINQ
- O(n^2) worst case, O(n) best case with optimization
- O(1) extra space