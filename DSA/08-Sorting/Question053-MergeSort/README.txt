Implement Merge Sort on an integer array.
Merge Sort divides the array in half, recursively sorts each half,
then merges the sorted halves.

Restrictions:
- Implement from scratch, do not use Array.Sort() or LINQ
- O(n log n) time complexity
- O(n) extra space is acceptable (merge step)