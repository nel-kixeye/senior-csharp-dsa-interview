Implement Selection Sort on an integer array.
Selection Sort divides the array into a sorted and unsorted part,
and repeatedly selects the minimum from the unsorted part.

Restrictions:
- Implement from scratch, do not use Array.Sort() or LINQ
- O(n^2) time complexity
- O(1) extra space