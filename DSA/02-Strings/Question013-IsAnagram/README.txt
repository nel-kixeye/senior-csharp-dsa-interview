Given two strings s and t, return true if t is an anagram of s.
An anagram uses all the same characters in the same quantities, just rearranged.

Restrictions:
- Do not use LINQ or Array.Sort()
- O(n) time complexity
- O(1) extra space (assume lowercase English letters only)