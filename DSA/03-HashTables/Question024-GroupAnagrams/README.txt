Given an array of strings, group the anagrams together.
Return a list of groups. Order within groups and order of groups does not matter.

Restrictions:
- Do not use LINQ or Array.Sort()
- O(n * k) time where k is the max string length