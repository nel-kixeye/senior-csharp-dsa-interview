Given an integer array, return true if any value appears at least twice.
Return false if every element is distinct.

Restrictions:
- Do not use LINQ
- O(n) time complexity
- O(n) space (hash set allowed)