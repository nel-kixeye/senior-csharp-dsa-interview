Given two sorted integer arrays, merge them into a single sorted array.
Return a new array (do not merge in-place).

Restrictions:
- Two-pointer technique
- O(m + n) time complexity
- Do not use LINQ or Array.Sort()