Given two integer arrays, return an array of their intersection
where each element appears as many times as it shows in both arrays.

Example: { 1, 2, 2, 1 } and { 2, 2 } -> { 2, 2 }

Restrictions:
- Do not use LINQ
- O(m + n) time complexity using a hash map