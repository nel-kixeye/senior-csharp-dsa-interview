try
{
    var result = Solution.IntersectWithDuplicates(new[] { 1, 2, 2, 1 }, new[] { 2, 2 });
    Console.WriteLine(string.Join(", ", result));
}
catch (NotImplementedException)
{
    Console.WriteLine("Solution not yet implemented.");
}