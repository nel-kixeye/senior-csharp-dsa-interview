Rotate an integer array to the right by k steps, in-place.

Example: { 1, 2, 3, 4, 5, 6, 7 } rotated by 3 becomes { 5, 6, 7, 1, 2, 3, 4 }

Restrictions:
- Do not use LINQ
- O(n) time complexity preferred
- O(1) extra space preferred
- Handle k larger than array length