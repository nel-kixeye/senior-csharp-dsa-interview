Given a sorted integer array, remove duplicates in-place so each element appears only once.
Return the count of unique elements k.
The first k elements of the array must contain the unique values in order.

Restrictions:
- Modify in-place, O(1) extra space
- Do not use LINQ
- O(n) time complexity