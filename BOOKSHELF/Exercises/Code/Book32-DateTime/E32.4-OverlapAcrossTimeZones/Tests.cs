using Xunit;

public class OverlapsTests
{
    [Fact]
    public void ExampleCase()
    {
        var actual = Solution.Overlaps(default, default, default, default);        Assert.True(actual);
    }
}

