Given the head of a linked list, determine if it contains a cycle.
A cycle exists if a node's Next pointer points to a previously visited node.

Restrictions:
- Do not use a HashSet (O(1) space solution required)
- Use Floyd's cycle detection algorithm (slow/fast pointers)
- O(n) time, O(1) space