Given a graph represented as an adjacency list and a start node,
perform a Breadth-First Traversal and return the visited nodes in order.

Restrictions:
- Use a Queue manually (or Queue<T>)
- Do not use LINQ
- O(V + E) time complexity