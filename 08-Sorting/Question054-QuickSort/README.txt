Implement Quick Sort on an integer array in-place.
Quick Sort picks a pivot, partitions the array around it,
and recursively sorts each partition.

Restrictions:
- Implement from scratch, do not use Array.Sort() or LINQ
- O(n log n) average, O(n^2) worst case
- O(log n) space on call stack (in-place)